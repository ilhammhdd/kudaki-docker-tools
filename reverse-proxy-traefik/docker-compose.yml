version: "3.3"

services:
  traefik_service:
    image: traefik
    container_name: traefik_container
    command: --docker --logLevel=ERROR
    ports:
      - "80:80"
      - "443:443"
    networks:
      - kudaki-docker-net
    volumes:
      - ./acme.json:/acme.json
      - ./traefik.toml:/traefik.toml
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      CLOUDFLARE_EMAIL: milham939@gmail.com
      CLOUDFLARE_API_KEY: dd7722f5e6d5da663b117d4ee7fa3b70cf76d
    restart: on-failure

networks:
  kudaki-docker-net:
    external: true